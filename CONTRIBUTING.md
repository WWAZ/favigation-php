# Contributing

Thanks for contributing to Favigation.

## Setup

1. Install dependencies:
   - `composer install`
2. Run tests:
   - `composer test`
3. Run static analysis:
   - `composer analyse`

## Pull Requests

- Keep changes backward compatible for public APIs.
- Add or update tests for behavior changes.
- Update `README.md` for user-facing changes.
